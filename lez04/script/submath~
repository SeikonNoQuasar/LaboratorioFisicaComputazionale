#! /bin/bash
(sleep 1s; time math <$1.m)> $1.out &	#lo sleep e` ridondante, ma e` solo per mostrare che qualche volta si vuole posticipare la partenza di un comando
					#e questo e` il modo

					#time mi permette di misurare quanti secondi sono stati consumati per eseguire il comando che segue
					#questo programma lancia matematica e le dice di caricare (il minore indica "leggi da", opposto di > che invece fa
					#output) il file $1 a cui aggiunge .m, e lascia in output un file $1 con .out
